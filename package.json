{
  "name": "circula",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter frontend start",
    "lint": "bun run --filter frontend lint",
    "build": "bun run --filter frontend build"
  }
}
